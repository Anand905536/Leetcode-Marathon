  vector<int> getRow(int rowIndex) {
           vector<int>ans;
        int n=rowIndex;
    long long num=1;
    for(int i=0;i<=n;i++)
    {
        ans.push_back(num);
         num=num*(n-i)/(i+1);
    }
    return ans;
