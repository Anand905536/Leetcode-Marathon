     int n=nums.size();
      unordered_map<int,int>mp;
   for(int i=0;i<n;i++)mp[nums[i]]++;
   int sum=0;
   for(auto it:mp)
   {
      if(it.second>1)
      {
         sum+=((it.second-1)*(it.second))/2;
      }

   }

   return sum;
    }
	```
